<IfModule mod_rewrite.c>
    RewriteEngine On

    # Pastikan file di dalam "build" bisa diakses langsung
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    RewriteRule ^ index.php [L]
</IfModule>